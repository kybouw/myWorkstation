[core]
    editor = vim
    abbrev = 7
	autocrlf = input
	safecrlf = true
[push]
    default = simple
[user]
    name = "Kyle Bouwman"
    email = "my school email"
[alias]
    co = checkout
    ci = commit
    st = status
    br = branch
    hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
    type = cat-file -t
    dump = cat-file -p
	pom = push --tags origin master
